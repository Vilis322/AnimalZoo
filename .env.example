# AnimalZoo Docker Environment Variables
# Copy this file to .env and customize as needed
# The .env file is git-ignored for security

# ============================================
# SQL Server Configuration
# ============================================

# Container name for SQL Server
# This name is used by Docker Compose and Makefile commands
DOCKER_NAME=animalzoo-sqlserver

# SQL Server SA (System Administrator) password
# Requirements:
#   - At least 8 characters long
#   - Must contain uppercase letters (A-Z)
#   - Must contain lowercase letters (a-z)
#   - Must contain numbers (0-9)
#   - Must contain special characters (!@#$%^&*()_+-=[]{}|;:,.<>?)
# Default: YourStrong@Passw0rd 
SA_PASSWORD=YourStrong@Passw0rd

# SQL Server port mapping
# Maps container port 1433 to host port
# Default: 1433
# Change this if port 1433 is already in use on your system
DB_PORT=1433

# ============================================
# Notes
# ============================================

# Security Best Practices:
# 1. Never commit .env to version control
# 2. Use a strong, unique password for SA_PASSWORD
# 3. Rotate passwords regularly
# 4. Restrict database access to localhost in development

# Port Conflicts:
# If port 1433 is already in use, change DB_PORT to another value (e.g., 1434)
# Then update the connection string in appsettings.json accordingly

# Connection String Format:
# Server=localhost,{DB_PORT};Database=AnimalZooDB;User Id=sa;Password={SA_PASSWORD};TrustServerCertificate=True;Encrypt=True;

# Makefile Override:
# You can also override these values when running make commands:
# make docker-up SA_PASSWORD="MyCustomPassword123!" DB_PORT=1434
